class NumberSequenceCode:
    def __init__(self, number_sequence_code, scope_type, scope_value, annotated_format, cleanup, company, 
                 continuous, cyclical, date_time_format, interval, in_use, largest, manual, name, next_, 
                 operating_unit_types, preallocation, quantity_of_numbers, skip_counting, smallest, stopped, 
                 to_a_higher_number, to_a_lower_number):
        self.number_sequence_code = number_sequence_code
        self.scope_type = scope_type
        self.scope_value = scope_value
        self.annotated_format = annotated_format
        self.cleanup = cleanup
        self.company = company
        self.continuous = continuous
        self.cyclical = cyclical
        self.date_time_format = date_time_format
        self.interval = interval
        self.in_use = in_use
        self.largest = largest
        self.manual = manual
        self.name = name
        self.next = next_
        self.operating_unit_types = operating_unit_types
        self.preallocation = preallocation
        self.quantity_of_numbers = quantity_of_numbers
        self.skip_counting = skip_counting
        self.smallest = smallest
        self.stopped = stopped
        self.to_a_higher_number = to_a_higher_number
        self.to_a_lower_number = to_a_lower_number